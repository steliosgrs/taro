# Copy to .env (or export) to configure the server.
TARO_DATABASE_URL=sqlite://taro.db
TARO_BIND=0.0.0.0:8080
TARO_BLOB_ROOT=./taro_blobs
# Leave unset/empty to disable auth (POC default). Set to enable the bearer stub.
# TARO_API_KEY=dev-token
RUST_LOG=taro_server=debug,tower_http=info
